【任务目标】
我需要训练一个轻量级的图像三分类模型，用于在 ESP32-S3 嵌入式设备上部署运行。请帮我完成从数据加载到模型导出的全流程。缺少库告诉我

【分类类别】
图像分为以下 2 个类别：
1. screw（螺丝）
3. washer（垫片）

【数据集说明】
训练数据存放在以下路径：
    /dataset/螺丝 450张
    /dataset/螺母 700张
从中挑选或随机分别选出400张做训练 50张做测试

图片格式为 JPG，原始分辨率为 640x480。

【模型要求】
- 使用 MobileNetV2 作为骨干网络（轻量级，适合嵌入式部署）
- 加载 ImageNet 预训练权重（迁移学习）
- 替换最后的全连接层为 3 分类输出
- 模型参数量控制在 3-5 MB 以内

【训练配置】
- 冻结骨干网络的所有层（特征提取策略），只训练最后的全连接层
- 输入图像尺寸：224x224
- 批量大小（batch size）：32
- 训练轮数（epochs）：20-30
- 优化器：Adam，学习率 0.001
- 损失函数：交叉熵损失（CrossEntropyLoss）

【数据增强策略】
请在训练时使用以下数据增强（参考 torchvision.transforms）：
- RandomResizedCrop(224, scale=(0.8, 1.0))
- RandomRotation(20)
- RandomHorizontalFlip()
- ColorJitter(brightness=0.2, contrast=0.2)
- ToTensor()
- Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])

验证集仅使用：
- Resize(256)
- CenterCrop(224)
- ToTensor()
- Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])

【评估标准】
- 在验证集上计算准确率（Accuracy）
- 目标验证准确率：> 95%

【输出要求】
请帮我输出以下文件：
1. 训练好的模型权重文件（.pth 格式）
2. 将模型导出为 ONNX 格式（输入尺寸固定为 1x3x224x224）
3. 训练过程的损失曲线和准确率曲线图
4. 一份简要的训练报告，包含最终验证准确率

【额外约束】
- 请确保代码可以在 Python 3.12 环境下运行
- 所有依赖库请列在 requirements.txt 中
- 训练完成后请打印模型的参数量（总参数个数）